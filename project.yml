name: TimHortonsRun
options:
  bundleIdPrefix: com.diongusija
settings:
  base:
    SWIFT_VERSION: 5.0
    IPHONEOS_DEPLOYMENT_TARGET: 16.0
targets:
  TimHortonsRun:
    type: application
    platform: iOS
    deploymentTarget: "16.0"
    sources:
      - path: TimHortonsRun
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.diongusija.TimHortonsRun
        INFOPLIST_FILE: TimHortonsRun/Info.plist
        CURRENT_PROJECT_VERSION: 1
        MARKETING_VERSION: 1.0
        CODE_SIGN_STYLE: Automatic
        GENERATE_INFOPLIST_FILE: NO
    scheme:
      testTargets: []
